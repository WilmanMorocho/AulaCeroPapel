En la carpeta de librerias se encuentran los archivos jar para que se pueda ejecutar el java

En la carpeta DDL se encuentra el script que se genera al exportar la base de datos

En el rar se encuentra el programa en java